[DllImport( "LLMath.dll", CallingConvention = CallingConvention.Cdecl )]
private static extern int arrayTest( [In, Out, MarshalAs( UnmanagedType.LPArray )]double[,] A );

/* A C# function that calls the unmanaged function */
private void ArrayCall( )
{
    double[,] A = new double[10,10];

    for ( int row=0; row<10; row++ )
    {
        for ( int col=0; col<10; row++ )
        {
            A[row,col] = 10*row + col;
        }
    }
}

/* An example unmanaged function that could be used here */
extern "C" __declspec(dllexport) int arrayTest( double* A )
{
    int row;
    int col;

    for ( row=0; row<10; row++ )
    {
        for ( col=0; col<10; col++ )
        {
            /* this would act as expected by the variable names */
            double c = A[10*row + col];
        }
    }
}


